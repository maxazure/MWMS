<h1>项目列表</h1>

<%= link_to '新建专题', new_admin_itemcontent_path %>

<table>
  <tr>
    <th>标题</th>

    <th>类别</th>
<th>操作</th>
  </tr>

<% @itemcontents.each do |itemcontent| %>
  <tr>
    <td><%=h itemcontent.title %></td>

    <td><%=h itemcontent.catalog_id %></td>


    <td><%= link_to '编辑', edit_admin_itemcontent_path(itemcontent) %>
   <!-- |   <%= link_to 'Destroy', [:admin,itemcontent], :confirm => 'Are you sure?', :method => :delete %></td>
  --></tr>
<% end %>
</table>

<br />

